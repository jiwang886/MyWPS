### 项目名称：MyWPS (基于 Qt 的多文档文本编辑器)

**项目描述：**
本项目是一个基于 C++ 和 Qt 框架开发的桌面文字处理软件。采用 MDI（多文档界面）架构，支持同时打开和编辑多个文本文档。项目实现了类似 WPS/记事本的核心功能，包括文件管理、文本编辑、格式设置以及多窗口管理，并解决了多编码格式（GBK/UTF-8）的兼容性问题。

**主要功能 (Features)：**

1.  **多文档管理 (MDI Architecture)：**
    *   支持同时创建、打开多个文档窗口。
    *   实现了子窗口的层叠 (Cascade)、平铺 (Tile)、关闭及切换功能。
    *   利用 `QMdiArea` 和 `QMdiSubWindow` 管理文档生命周期。

2.  **文件操作与编码支持：**
    *   实现了标准的文件新建、打开、保存、另存为功能。
    *   **多编码支持**：针对中文环境优化，能够正确识别和处理 GBK 与 UTF-8 编码的文本文件（项目中包含 `长恨歌_gbk.txt` 等测试用例），避免乱码。

3.  **文本编辑与排版：**
    *   **基础编辑**：支持撤销 (Undo)、重做 (Redo)、复制、剪切、粘贴。
    *   **格式设置**：支持设置字体类型、字号、加粗、倾斜、下划线以及字体颜色。
    *   **段落对齐**：支持左对齐、居中、右对齐和两端对齐。

4.  **UI 与交互：**
    *   使用 Qt Designer (`.ui` 文件) 设计图形界面，包含菜单栏、工具栏和状态栏。
    *   集成了自定义图标资源 (`res.qrc`)，提升界面美观度。
    *   实现了文档打印及打印预览功能（推测基于 `QPrinter`）。

**技术栈 (Tech Stack)：**
*   **语言**：C++ (C++11/17)
*   **框架**：Qt 5/6 (Qt Widgets)
*   **构建工具**：CMake (包含 `CMakePresets.json` 配置，支持跨平台构建)
*   **核心类**：`QMainWindow`, `QMdiArea`, `QTextEdit`/`QTextDocument`, `QFile`, `QTextStream`
